syntax = "proto3";

package UserRelated;

// User Information
message UserInfo {
    string UID = 1;
    string name = 2;
}

// Group Information
// UIDs is all the users' UID in this group.
message GroupInfo {
    string GroupID = 1;
    string name = 2;
    repeated string UIDs = 3;
}